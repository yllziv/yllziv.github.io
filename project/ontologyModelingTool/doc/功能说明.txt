
本体框：
1. 显示本体图
2. 显示本体信息
3. 添加本体（判断是否重复，是否为空）·
4. 将本体设为某一节点的子节点，并自动判断大小、树的深度、颜色等信息
5. 本体输入框自动提示
6. 显示某本体及该本体的上下级节点（判断是否存在，是否为空）
7. 删除节点
8. 动态更新输入框

（圆：大小改为Text长度，
显示子节点和父节点时需要刷新页面
高度设置响应（自适应）
子节点的getX需要调整
）

地图框：
1. 显示地图
2. 切换楼层
3. 2/3维切换
4. 显示不同元素（未完成）
5. 显示和编辑属性信息（未完成）

实例框：
1. 显示所有实例
2. 实例输入框自动提示
3. 添加实例（判断是否重复,是否为空）       

4. 动态更新输入框
5. 点击实例弹出属性对话框
（
移动到列表，右边出现edit选项
单击：高亮——地图单击——高亮：关联起来
）

属性对话框
1. 选择属性类型，对象属性或者是数据属性
2. 添加属性（判断是否存在，是否重复）
3. 删除属性
4. 输入框自动提示
（属性显示用表格，属性框选择classType
对象属性填写下拉框——对象）



